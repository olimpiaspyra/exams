// selectors

// action name creator

// action types

// action creators

// reducer
const examsReducer = (statePart = [], action = {}) => {
  switch (action.type) {
    default:
      return statePart;
  }
};

export default examsReducer;
